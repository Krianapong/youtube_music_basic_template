import 'package:flutter/cupertino.dart';

class LibraryMusicScreen extends StatelessWidget {
  const LibraryMusicScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Library Music"),
    );
  }
}
